import os
import nbconvert

# Folder containing your notebooks
notebook_folder = "models"

# Loop through all .ipynb files in the folder
for file in os.listdir(notebook_folder):
    if file.endswith(".ipynb"):
        notebook_path = os.path.join(notebook_folder, file)
        os.system(f"jupyter nbconvert --to script {notebook_path}")
